@model vezba_lab3.Models.Doctor

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Doctor</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
</div>

@Html.ActionLink("Опеделете болница", "AssignHospital", new { Id = Model.Id })

<ul>
    @foreach (var patient in Model.Patients)
    {
        <li>
            @Html.ActionLink(patient.Name, "Details/" + patient.Id, "Patients")
        </li>
    }
</ul>

@Html.ActionLink("Внесете пациент", "InsertPatient", new { Id = Model.Id })

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
